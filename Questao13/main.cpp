#include <iostream>
using namespace std;
// Cria uma variável do tipo char chamada letra
// Use cout para pedir que seja digitada uma letra
// Use cin para ler a letra para a variável letra
// Use cout para imprimir o resultado da operação
// (char)(letra ^ (1 << 5))
// Note que ^ é o operador bit-a-bit do ou-exclusivo
// Se a letra digitada for maiúscula se tornará minúscula
// e vice-versa.
int main() {
  cout << "Hello world!" << endl;
  return 0;
}
